import styles from "./Header.module.css";

const Header = () => {
  return (
    <header id={styles.header_container}>
      <h2>Countries Sample App</h2>
    </header>
  );
};

export default Header;
